# TerraFirmaGreg Modern v0.11.28 — GitHub release server pack
server:
  output_dir: ${E2E_OUTPUT_DIR}/tfg
  eula: true
  jvm:
    memory: 4G

modpack:
  platform: github
  source:
    repo: TerraFirmaGreg-Team/Modpack-Modern
    tag: 0.11.28
    asset: "*serverpack*"
