# All of Create — CurseForge modpack
# https://www.curseforge.com/minecraft/modpacks/aoc
# Requires CF_API_KEY environment variable
server:
  output_dir: ${E2E_OUTPUT_DIR}/aoc
  eula: true
  memory: 4G

modpack:
  platform: curseforge
  source:
    slug: aoc
    api_key: ${CF_API_KEY}
