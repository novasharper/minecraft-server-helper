# Cobblemon — Modrinth modpack (Fabric)
# https://modrinth.com/modpack/cobblemon-fabric
server:
  output_dir: ${E2E_OUTPUT_DIR}/cobblemon
  eula: true
  jvm:
    memory: 4G

modpack:
  platform: modrinth
  source:
    project: cobblemon-fabric
    version_type: release
